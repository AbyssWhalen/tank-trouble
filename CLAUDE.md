# 坦克回廊

本地多人 + 人机对战的坦克对战小游戏（玩法参考 Tank Trouble，代码与素材全部原创）。HTML5 Canvas + 原生 JS，Electron 套壳发布为 Windows 桌面 App。

## 技术栈
- 游戏本体：HTML5 Canvas + 原生 JavaScript（ES Modules），零运行时依赖
- 桌面外壳：Electron
- 打包：electron-builder（target=nsis，生成 Windows 安装向导式 .exe）

## 目录结构约定
- `electron/` —— Electron 主进程代码（`.cjs`，CommonJS）
- `src/` —— 游戏逻辑模块（ES Modules，浏览器/渲染进程里跑）
- `assets/` —— 图标、音效、图片
- `build/` —— electron-builder 输出，已 gitignore，不要手动改
- `index.html` —— 渲染进程入口，挂 canvas
- 新增模块放 `src/`，一个文件一个职责，文件名小写

## 模块职责
- `config.js` —— 全局常量：格子尺寸、速度、地图档位(`MAZE_TIERS`)、模式→档位池(`TIER_POOL_BY_MODE`)、AI 共享参数(`AI`)与难度三档(`AI_DIFFICULTY`)、爆炸特效(`EXPLOSION`)、视口预留、键位表、颜色
- `maze.js` —— 随机地图生成（稀疏格栅：概率放墙 + 泛洪连通修复），输出墙线段数组
- `collision.js` —— 纯几何工具：圆-线段碰撞、线段-线段相交（布尔/参数版）、等质量双圆分离、反射向量、距离判定
- `layout.js` —— 竞技场自适应缩放：纯函数 `fitArena`，给定地图世界尺寸算缩放比+居中偏移（小图原样、大图等比缩小）
- `tank.js` —— 坦克：位置/朝向/移动/撞墙滑动；`tryFire` 受 maxAlive 限流（需传入全局子弹数组）+ 贴墙出膛修正（炮口越墙时压回墙内侧，防穿墙）
- `bullet.js` —— 子弹：直线运动 + 墙面反射 + 反弹次数/寿命；出膛宽限只保护未反弹的子弹（贴墙开炮弹回来会打死自己，原版自杀手感）；渲染为原版风格小黑点
- `input.js` —— 键盘状态，多套键位分发到对应玩家；`readControls` 把键位转成统一控制指令 `{turn, move, fire}`
- `player.js` —— 玩家：绑定坦克 + 控制源（人类键盘 / AI 控制器）+ 颜色（单回合实体；累计分外置在 main 的 `matchScores`）
- `ai.js` —— AI 控制器，接口与 input 对齐（每帧产出同构控制指令）：八向安全评分躲弹（全弹幕相对运动外推选最优航向，航向锁定防抖、朝墙重罚、可倒车）、有视线直追、没视线 BFS 走格子图、开火三道门（节奏冷却 + 弹药预算 + 命中质量=几何必中角×难度技巧系数，近大远小所以近战边追边连发）、卡住自动倒车脱困；难度三档（easy/normal/hard，菜单 chip 单选）只调决策参数不动物理，简单档不躲弹
- `effects.js` —— 视觉特效（世界坐标，纯表现层）：坦克被击破的爆炸=深色烟团膨胀 + 浅色碎片四散自旋，done 后由 main 移除；联机 v2 复用
- `main.js` —— 主循环 + 状态机（菜单/对战/回合结算）；坦克间碰撞分离（不可重叠，推开后贴墙修正）；画布 HiDPI + 小窗等比缩放（永不裁切）；持有跨回合的 `matchScores` 与竞技场缩放 `arenaScale`

## 坐标与单位约定
- 世界坐标以像素为单位，原点在 canvas 左上角
- 角度用弧度，0 指向右（+x），顺时针为正
- 迷宫以「格」为逻辑单位，渲染时乘 `CELL_SIZE` 转像素
- 物理（碰撞/反弹/滑动）全用世界坐标，与渲染解耦；竞技场缩放只在 render 层 `ctx.scale`，不影响物理

## 命令
- `npm start` —— 开发模式启动 Electron 窗口
- `npm run dist` —— 打包出 Windows 安装包到 `build/`

## 验证纪律
- 无自动化测试，靠浏览器/Electron 手动验证
- 每阶段完成后必须：窗口能开、控制台无报错、该阶段验收点逐条过
- 改完主动跑 `npm start` 看效果，不要只改不验

## 开发分期（按序推进，每期可独立验证）
0. 项目骨架 + Electron 空窗口 ✅
1. 坦克渲染 + 单套键位移动转向 ✅
2. 随机迷宫 + 撞墙不穿 ✅
3. 开炮 + 子弹反弹 + 击中判定 ✅
4. 双人对战：菜单 + 多键位 + 胜负横幅 ✅（子弹已按原版调校：小黑点 + maxAlive 限流）
5. 回合制 + 计分 + 重开 ✅（分数外置 matchScores + 自动倒计时重开 + HUD 比分）
6. 人机模式 + 基础 AI ✅（控制指令抽象 `{turn,move,fire}` 解耦人机输入；AI = 八向评分躲弹 + 视线直追 + BFS 绕路 + 开火三道门（节奏/弹药预算/命中质量）+ 卡住脱困；菜单可选难度三档，简单档不躲弹；坦克间碰撞不可重叠）
7. 打包发布 Windows .exe

## 路线图调整说明
- 本地版聚焦 **1v1 双人对战 + 人机**：3/4 人同屏挤一台键盘的玩法已弃用。
- **3/4 人对战归入联机 v2**（独立大版本，需信令/同步服务器，本地版完成后再设计）。
- config 里第 3/4 套键位、`TIER_POOL_BY_MODE` 的 `3p`/`4p` 池及 `large` 档暂留不删，供 v2 复用。
- 地图改档位制（`MAZE_TIERS` small/medium/large + `TIER_POOL_BY_MODE` 按模式抽池）：pvp/pve 抽 small/medium，每回合换图；large 已就绪（含自适应缩放），本地版抽不到，仅冒烟测试覆盖。
